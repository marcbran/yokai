version: 2

project_name: yokai

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

dockers:
  - image_templates:
      - "{{ if not .IsSnapshot }}ghcr.io/marcbran/yokai:{{ .Tag }}{{ end }}"
      - "{{ if not .IsSnapshot }}ghcr.io/marcbran/yokai:{{ .Major }}{{ end }}"
      - "{{ if not .IsSnapshot }}ghcr.io/marcbran/yokai:{{ .Major }}.{{ .Minor }}{{ end }}"
      - "{{ if not .IsSnapshot }}ghcr.io/marcbran/yokai:latest{{ end }}"
      - "{{ if .IsSnapshot }}ghcr.io/marcbran/yokai:{{ .Tag }}-SNAPSHOT{{ end }}"
      - "{{ if .IsSnapshot }}ghcr.io/marcbran/yokai:dev{{ end }}"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
